{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Filters",
            "file": "${workspaceFolder}/filters.html"
        }
    ]
}